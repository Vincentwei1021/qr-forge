Read TASK.md in this directory and implement everything described in it. Build the complete QR Forge website. The project is already initialized with Next.js + Tailwind + qrcode package. Build all components (Header, Hero, QRGenerator client component, FAQ accordion, Footer), the homepage at / with all sections, blog placeholder at /blog, API route at /api/qr, full SEO (meta tags, JSON-LD WebApplication schema, OG tags, sitemap.ts, robots.ts), mobile-first responsive design with indigo primary color, client-side QR generation as primary method, download for PNG/SVG, color pickers, size/margin sliders, format toggle, and a batch generation Coming Soon teaser. After building everything, run npm run build to verify it compiles successfully.
